import { RolesItemDirective } from './roles-item.directive';

describe('RolesItemDirective', () => {
  it('should create an instance', () => {
    const directive = new RolesItemDirective();
    expect(directive).toBeTruthy();
  });
});
